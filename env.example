# ========================
# 🔐 OAuth Providers
# ========================

# GitHub OAuth App credentials (from GitHub Developer Settings > OAuth Apps)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth credentials (from Google Cloud Console > Credentials > OAuth 2.0 Client IDs)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret


# ========================
# 🌐 App Config
# ========================

# The base URL of your app (use localhost in dev, production domain in prod)
# Example: http://localhost:3000 (dev) or https://yourdomain.com (prod)
BASE_URL=http://localhost:3000

# Secret key used by NextAuth for signing & encrypting tokens
# Generate with: `openssl rand -base64 32`
AUTH_SECRET=your_generated_auth_secret


# ========================
# 🗄️ Database
# ========================

# Postgres database URL (from Neon project dashboard)
# Format: postgres://user:password@host/dbname
DATABASE_URL=postgresql://username:password@ep-neonhost-db.neon.tech/dbname?sslmode=require
